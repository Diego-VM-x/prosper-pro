/**
 * Seed vacío - Prosper-Pro inicia sin datos de ejemplo.
 * Todos los datos deben ser creados por el usuario.
 */

export async function seedUserData(_userId: string) {
  // Sin datos de ejemplo. El usuario crea su propia información.
  console.log('Seed desactivado: sin datos de ejemplo');
}
